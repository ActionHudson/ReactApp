import {
    Stack
} from '@mantine/core';

export default function Settings () {

    return (
        <Stack style={ {
            backgroundColor: 'white',
            padding: '1rem',
            borderRadius: '0.5rem',
            height: '100%',
            display: 'flex',
            flexDirection: 'column'
        } }>

        </Stack>
    );
}
